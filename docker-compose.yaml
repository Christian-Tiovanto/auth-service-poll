version: '3.8'

services:
  database:
    image: postgres:17.6
    container_name: poll_database
    environment:
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_USER: ${DB_USER}
    ports:
      - '${DB_PORT}:5432'
    restart: always
    tty: true

volumes:
  db_data:
